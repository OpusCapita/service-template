{
  "name": "opuscapita-{{your-service-name}}",
  "version": "1.0.0",
  "description": "{{service-description}}",
  "author": "{{your-name}}",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/OpusCapita/{{your-service-name}}"
  },
  "bugs": {
    "url": "https://github.com/OpusCapita/{{your-service-name}}/issues"
  },
  "homepage": "https://github.com/OpusCapita/{{your-service-name}}",
  "nyc": {
    "exclude": [
      "test"
    ]
  },
  "dependencies": {
    "bluebird": "^3.5.0",
    "ocbesbn-config": "^2.2.6",
    "ocbesbn-db-init": "^1.1.9",
    "ocbesbn-logger": "^1.0.2",
    "ocbesbn-redis-events": "^1.0.4",
    "ocbesbn-web-init": "^2.1.2",
    "ocbesbn-bouncer": "^0.2.11",
    "sequelize": "^4.0.0"
  },
  "devDependencies": {
    "jsdoc-to-markdown": "^3.0.0",
    "mocha": "^3.2.0",
    "mocha-junit-reporter": "^1.12.1",
    "nodemon": "^1.11.0",
    "nyc": "^10.2.2",
    "raml-to-markdown": "^1.1.4",
    "rimraf": "^2.6.0",
    "sequelize-to-markdown": "^1.1.13"
  },
  "scripts": {
    "start": "node src/server/index.js",
    "dev": "npm run setup-consul ; npx nodemon --watch src/server src/server/index.js",
    "test": "npm run clean ; npm run setup-consul ; npx nyc mocha --recursive -R mocha-junit-reporter",
    "clean": "npx rimraf .nyc_output coverage test-results.xml",
    "setup-consul": "sh setup-consul.sh",
    "rest-doc": "mkdir -p wiki/rest-doc ; npx raml2md -o file-per-resource -p wiki/rest-doc --output-ext=.endpoint.md rest-doc/main.raml",
    "domain-doc": "mkdir -p wiki/domain-doc ; npx sq2md -o file-per-src -p wiki/domain-doc --init=init --field-bl=createdAt,updatedAt --output-ext=.model.md src/server/db/models",
    "api-doc": "mkdir -p wiki/api-doc ; npx jsdoc2md -f $(find src/server) > wiki/api-doc/Home.md",
    "doc": "npm run rest-doc ; npm run domain-doc ; npm run api-doc"
  }
}
